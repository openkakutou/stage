# Module: root
**Role:** Package `stage` at the module root — currently only the library version constant; the stage data model (`BGdef`, BG elements/layers, camera bounds, boundaries), `.def` parse/serialize, `sff` sprite integration, and BG element frame/animation resolution will land here as the backlog (items 001-006) is worked.
**Files:** `version.go`
**Exports:** `Version` (string constant)
**Depends on:** nothing yet (stdlib-only for now; `github.com/openkakutou/sff` arrives with backlog item 004)
