// Package stage reads and writes MUGEN/Ikemen GO stage (background) .def
// files: BGdef, BG elements/layers, camera bounds, and stage boundaries. It
// has no rendering dependency and compiles to WebAssembly.
package stage

// Version is the library version.
const Version = "0.2.0"
